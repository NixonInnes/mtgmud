* Add user.listening, and ignored lists, so users can mute channels/players

* Add function to watch other connected users
    - Add check in user.msg_user to see if there are users in their user.spectator list, and send to that user too

* Add user list of allowed spectators (user.db.id) { if user.db.id in u.spectators }
    - Will need to be saved to the db

* check for empty chats
